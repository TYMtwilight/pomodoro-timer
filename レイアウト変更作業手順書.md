# ポモドーロタイマー レイアウト変更 作業手順書

**期限**: 2026年1月31日
**作成日**: 2026年1月17日

---

## 目次

1. [事前準備](#1-事前準備)
2. [Phase 1: コンポーネント構造変更](#phase-1-コンポーネント構造変更)
3. [Phase 2: レイアウト実装](#phase-2-レイアウト実装)
4. [Phase 3: 装飾要素の実装](#phase-3-装飾要素の実装)
5. [Phase 4: レスポンシブ調整](#phase-4-レスポンシブ調整)
6. [テスト手順](#6-テスト手順)
7. [デプロイ手順](#7-デプロイ手順)

---

## 1. 事前準備

### 1.1 作業環境の確認

```bash
# 開発サーバーを起動して現在の状態を確認
npm run dev
```

ブラウザで `http://localhost:3000/timer/focus` を開き、現在のレイアウトを確認。

### 1.2 Git ブランチの作成

```bash
# メインブランチから新しいブランチを作成
git checkout main
git pull origin main
git checkout -b feature/layout-redesign
```

### 1.3 必要な依存関係の確認

```bash
# package.jsonで以下のパッケージが含まれていることを確認
# - lucide-react（アイコン）
# - @/components/ui/* (shadcn/ui)
# - tailwindcss

# 不足している場合はインストール
npm install
```

---

## Phase 1: コンポーネント構造変更

**期間**: 1月17日-1月23日
**優先度**: 高

---

### Step 1.1: Header コンポーネントの変更

**所要時間**: 2-3時間

#### 1. 既存ファイルのバックアップ（オプション）

```bash
# 念のためバックアップを取る
cp components/header/Header.tsx components/header/Header.tsx.backup
```

#### 2. Header.tsx の変更

**ファイルパス**: `components/header/Header.tsx`

**既存のコードを以下に置き換え**:

```tsx
'use client';

import { useState } from 'react';
import { useSession, signOut } from 'next-auth/react';
import { User, LogOut } from 'lucide-react';
import Link from 'next/link';
import Image from 'next/image';
import { LoginButton } from './LoginButton';
import { Button } from '@/components/ui/button';
import { SettingsModal } from '@/components/settings/SettingsMordal';
import { TasksModal } from '@/components/tasks';

export function Header() {
  const { data: session, status } = useSession();
  const [isSettingsOpen, setIsSettingsOpen] = useState(false);
  const [isTasksOpen, setIsTasksOpen] = useState(false);

  /**
   * ログアウト処理
   */
  const handleLogout = () => {
    signOut({ callbackUrl: '/' });
  };

  return (
    <>
      <header className="w-full bg-transparent">
        <div className="flex items-center justify-between px-6 py-4">
          {/* 左: ユーザーアバター（ログイン時のみ） */}
          <div>
            {status === 'authenticated' ? (
              <Link
                href="/records"
                className="group flex items-center justify-center w-10 h-10 rounded-full overflow-hidden bg-gray-800 hover:ring-2 hover:ring-blue-400 transition-all"
                aria-label="作業記録を見る"
              >
                {session?.user?.image ? (
                  <Image
                    src={session.user.image}
                    alt={session.user.name || 'ユーザーアバター'}
                    width={40}
                    height={40}
                    className="w-full h-full object-cover"
                  />
                ) : (
                  <User className="w-6 h-6 text-white" />
                )}
              </Link>
            ) : status === 'unauthenticated' ? (
              <LoginButton />
            ) : (
              <div className="h-10 w-10 animate-pulse rounded-full bg-neutral-800" />
            )}
          </div>

          {/* 右: ログアウトボタン（ログイン時のみ） */}
          <div>
            {status === 'authenticated' ? (
              <Button
                onClick={handleLogout}
                variant="ghost"
                size="sm"
                className="flex items-center gap-2 text-white hover:text-red-400 hover:bg-transparent transition-colors"
              >
                <span className="text-sm font-medium">logout</span>
                <LogOut className="w-4 h-4" />
              </Button>
            ) : (
              // 未ログイン時は空白
              <div className="w-10" />
            )}
          </div>
        </div>
      </header>

      {/* 作業項目管理モーダル */}
      <TasksModal
        open={isTasksOpen}
        onOpenChange={setIsTasksOpen}
      />

      {/* 設定モーダル */}
      <SettingsModal
        open={isSettingsOpen}
        onOpenChange={setIsSettingsOpen}
      />
    </>
  );
}
```

**注意**: タスク名の表示はヘッダーではなく、メインコンテンツ（円形タイマーの上部）で行います。Phase 2 のタイマーページ変更で対応します。

#### 3. 動作確認

- 開発サーバーで確認
- ヘッダーが新しいデザインに変わっていることを確認（左: ユーザーアイコン、右: ログアウト）
- ユーザーアイコンをクリックして `/records` に遷移することを確認
- ログアウトボタンが動作することを確認

---

### Step 1.2: TimerControls の完全再設計

**所要時間**: 3-4時間

#### ボタン表示ルール

| タイマー状態 | 左ボタン | 右ボタン |
|-------------|---------|---------|
| **未開始**（hasStarted = false） | SETTINGS | START |
| **開始済み**（hasStarted = true） | RESET | STOP（実行中）/ START（停止中） |

**重要**: `hasStarted` は `useTimer` フックで管理されるステートです。
- START押下時に `true` になる
- RESET押下時に `false` に戻る
- 時間が初期値に戻っても、RESETを押さない限り `true` のまま

```
未開始（hasStarted = false）:
┌─────────────────────────────────────────────────┐
│ [SETTINGS]              [    START             ]│
└─────────────────────────────────────────────────┘

開始済み（hasStarted = true）:
┌─────────────────────────────────────────────────┐
│ [RESET   ]              [  START / STOP        ]│
└─────────────────────────────────────────────────┘
```

#### 1. 既存ファイルのバックアップ（オプション）

```bash
# 念のためバックアップを取る
cp components/timer/TimerControls.tsx components/timer/TimerControls.tsx.backup
```

#### 2. TimerControls.tsx の完全書き換え

**ファイルパス**: `components/timer/TimerControls.tsx`

**既存のコードを以下に置き換え**:

```tsx
'use client';

import { memo } from 'react';
import { Play, Pause, RotateCcw, Settings } from 'lucide-react';
import { Button } from '@/components/ui/button';

interface TimerControlsProps {
  isRunning: boolean;
  hasStarted: boolean;
  onToggle: () => void;
  onReset: () => void;
  onOpenSettings: () => void;
}

export const TimerControls = memo(function TimerControls({
  isRunning,
  hasStarted,
  onToggle,
  onReset,
  onOpenSettings,
}: TimerControlsProps) {

  /**
   * 左ボタンを返す
   * - 未開始（hasStarted = false）: SETTINGSボタン
   * - 開始済み（hasStarted = true）: RESETボタン
   */
  const renderLeftButton = () => {
    if (!hasStarted) {
      return (
        <Button
          variant="outline"
          className="h-14 rounded-lg border-2 border-blue-500 bg-transparent hover:bg-blue-500/10 text-white font-semibold transition-all duration-200 active:scale-95"
          onClick={onOpenSettings}
          aria-label="設定を開く"
        >
          <Settings className="w-5 h-5 mr-2" />
          SETTINGS
        </Button>
      );
    }

    return (
      <Button
        variant="outline"
        className="h-14 rounded-lg border-2 border-blue-500 bg-transparent hover:bg-blue-500/10 text-white font-semibold transition-all duration-200 active:scale-95"
        onClick={onReset}
        aria-label="タイマーをリセット"
      >
        <RotateCcw className="w-5 h-5 mr-2" />
        RESET
      </Button>
    );
  };

  return (
    <div className="grid grid-cols-2 gap-4 w-full max-w-md px-4">
      {/* 左: SETTINGS または RESET */}
      <div className="flex items-stretch">
        {renderLeftButton()}
      </div>

      {/* 右: START/STOP（大きめ） */}
      <div className="flex items-stretch">
        <Button
          size="lg"
          className="h-14 w-full rounded-lg bg-blue-600 hover:bg-blue-700 text-white font-bold text-lg transition-all duration-200 active:scale-95 active:bg-blue-800"
          onClick={onToggle}
          aria-label={isRunning ? 'タイマーを停止' : 'タイマーを開始'}
        >
          {isRunning ? (
            <>
              <Pause className="w-6 h-6 mr-2" />
              STOP
            </>
          ) : (
            <>
              <Play className="w-6 h-6 mr-2" />
              START
            </>
          )}
        </Button>
      </div>
    </div>
  );
});
```

#### 3. 型エラーの確認

```bash
# TypeScriptのエラーをチェック
npm run build
```

エラーがある場合は修正。

#### 4. 呼び出し元の修正

TimerControls に新しいプロップ（`timeLeft`, `initialTime`）を渡す必要があります。
タイマーページ（focus/page.tsx 等）の修正は Phase 2 で行います。

---

### Step 1.3: SettingsModal に TaskSelect を統合

**所要時間**: 2-3時間

#### 1. Separator コンポーネントの確認

```bash
# Separatorコンポーネントがない場合は追加
npx shadcn-ui@latest add separator
```

#### 2. SettingsModal.tsx の修正

**ファイルパス**: `components/settings/SettingsMordal.tsx`

**変更箇所1: インポートの追加（ファイル上部）**

```tsx
// 既存のインポートの後に追加
import { TaskSelect } from '@/components/tasks/TaskSelect';
import { Separator } from '@/components/ui/separator';
```

**変更箇所2: 作業項目選択セクションの追加**

`<div className="grid gap-4 py-4">` の直後に以下を追加:

```tsx
<div className="grid gap-4 py-4">
  {/* 作業項目選択（新規追加） */}
  <div className="space-y-2">
    <Label className="text-sm font-medium">作業項目</Label>
    <TaskSelect disabled={false} />
    <p className="text-xs text-muted-foreground">
      タイマー実行時に記録する作業項目を選択できます
    </p>
  </div>

  <Separator className="my-2" />

  {/* 既存の設定項目（作業時間など）はそのまま */}
  <div className="grid grid-cols-4 items-center gap-4">
    <Label htmlFor="focusTime" className="text-right">
      作業時間
    </Label>
    {/* ... 既存のコード ... */}
  </div>

  {/* 以降、既存のコードをそのまま維持 */}
</div>
```

#### 3. 動作確認

```bash
# 開発サーバーを起動
npm run dev
```

- ブラウザで設定モーダルを開き、作業項目選択が表示されることを確認
- 作業項目の選択・変更が正常に動作することを確認

---

### Step 1 のコミット

```bash
# 変更をコミット
git add components/header/Header.tsx
git add components/timer/TimerControls.tsx
git add components/settings/SettingsMordal.tsx
git commit -m "feat: Phase 1完了 - Header変更、TimerControls再設計、SettingsModalにTaskSelect統合"
```

---

## Phase 2: レイアウト実装

**期間**: 1月20日-1月23日
**優先度**: 高

---

### Step 2.1: タイマーページのレイアウト変更

**所要時間**: 4-5時間（3ページ分）

#### レイアウト構成

```
┌─────────────────────────────────────────────────┐
│ [ユーザーアイコン]                    [logout] │  ← ヘッダー（Header.tsx）
├─────────────────────────────────────────────────┤
│                                                 │
│              作業するタスク名                   │  ← メインコンテンツ
│                                                 │
│              ┌───────────┐                     │
│              │           │                     │
│              │  円形     │                     │
│              │  タイマー │                     │
│              │           │                     │
│              └───────────┘                     │
│                                                 │
│            現在のポモドーロ数                   │
│                                                 │
├─────────────────────────────────────────────────┤
│ [SETTINGS or RESET]     [    START/STOP    ]   │  ← ボタンエリア
└─────────────────────────────────────────────────┘
```

**重要**: 
- タスク名はヘッダーではなく、メインコンテンツ（円形タイマーの上）に表示します。
- 左ボタンはカウントダウン前は SETTINGS、カウントダウン後は RESET を表示します。

#### 1. Focus ページの変更

**ファイルパス**: `app/(public)/timer/focus/page.tsx`

**全体を以下のコードに置き換え**:

```tsx
'use client';

import { useState } from 'react';
import { useTimer } from '@/components/timer/useTimer';
import { TimerDisplay } from '@/components/timer/TimerDisplay';
import { TimerControls } from '@/components/timer/TimerControls';
import { Header } from '@/components/header/Header';
import { SettingsModal } from '@/components/settings/SettingsMordal';
import { useSession } from '@/contexts/SessionContext';
import { useSettings } from '@/contexts/SettingsContext';
import { useTasks } from '@/contexts/TaskContext';

export default function FocusPage() {
  const { settings } = useSettings();
  const { sessionCount, maxSessions } = useSession();
  const { tasks, selectedTaskId } = useTasks();
  const INITIAL_TIME = settings.focusTime * 60;

  const { timeLeft, isRunning, toggleTimer, resetTimer } = useTimer({
    initialTime: INITIAL_TIME,
    timerType: 'focus',
  });

  // 設定モーダルの状態
  const [isSettingsOpen, setIsSettingsOpen] = useState(false);

  // 選択された作業項目の名前を取得
  const selectedTask = tasks.find(t => t.id === selectedTaskId);
  const taskName = selectedTask?.name || '項目なし';

  return (
    <div className="flex flex-col min-h-screen bg-black text-white">
      {/* ヘッダー */}
      <Header />

      {/* メインエリア */}
      <div className="flex-1 flex flex-col items-center justify-center gap-8 px-4">
        {/* 作業項目名（円形タイマーの上） */}
        <h2 className="text-xl font-medium text-white tracking-wide">
          {taskName}
        </h2>

        {/* タイマー表示 */}
        <TimerDisplay
          timerType="focus"
          timeLeft={timeLeft}
          initialTime={INITIAL_TIME}
          sessionCount={sessionCount}
          maxSessions={maxSessions}
        />

        {/* ボタンエリア */}
        <TimerControls
          isRunning={isRunning}
          hasStarted={hasStarted}
          onToggle={toggleTimer}
          onReset={resetTimer}
          onOpenSettings={() => setIsSettingsOpen(true)}
        />
      </div>

      {/* 設定モーダル */}
      <SettingsModal
        open={isSettingsOpen}
        onOpenChange={setIsSettingsOpen}
      />
    </div>
  );
}
```

#### 2. Break ページの変更

**ファイルパス**: `app/(public)/timer/break/page.tsx`

**全体を以下のコードに置き換え**（focus ページと似ているが、`timerType`が異なる）:

```tsx
'use client';

import { useState } from 'react';
import { useTimer } from '@/components/timer/useTimer';
import { TimerDisplay } from '@/components/timer/TimerDisplay';
import { TimerControls } from '@/components/timer/TimerControls';
import { Header } from '@/components/header/Header';
import { SettingsModal } from '@/components/settings/SettingsMordal';
import { useSettings } from '@/contexts/SettingsContext';
import { useTasks } from '@/contexts/TaskContext';

export default function BreakPage() {
  const { settings } = useSettings();
  const { tasks, selectedTaskId } = useTasks();
  const INITIAL_TIME = settings.breakTime * 60;

  const { timeLeft, isRunning, toggleTimer, resetTimer } = useTimer({
    initialTime: INITIAL_TIME,
    timerType: 'break',
  });

  // 設定モーダルの状態
  const [isSettingsOpen, setIsSettingsOpen] = useState(false);

  // 選択された作業項目の名前を取得
  const selectedTask = tasks.find(t => t.id === selectedTaskId);
  const taskName = selectedTask?.name || '項目なし';

  return (
    <div className="flex flex-col min-h-screen bg-black text-white">
      {/* ヘッダー */}
      <Header />

      {/* メインエリア */}
      <div className="flex-1 flex flex-col items-center justify-center gap-8 px-4">
        {/* 作業項目名（円形タイマーの上） */}
        <h2 className="text-xl font-medium text-white tracking-wide">
          {taskName}
        </h2>

        {/* タイマー表示 */}
        <TimerDisplay
          timerType="break"
          timeLeft={timeLeft}
          initialTime={INITIAL_TIME}
        />

        {/* ボタンエリア */}
        <TimerControls
          isRunning={isRunning}
          hasStarted={hasStarted}
          onToggle={toggleTimer}
          onReset={resetTimer}
          onOpenSettings={() => setIsSettingsOpen(true)}
        />
      </div>

      {/* 設定モーダル */}
      <SettingsModal
        open={isSettingsOpen}
        onOpenChange={setIsSettingsOpen}
      />
    </div>
  );
}
```

#### 3. Long Break ページの変更

**ファイルパス**: `app/(public)/timer/long-break/page.tsx`

**全体を以下のコードに置き換え**:

```tsx
'use client';

import { useState } from 'react';
import { useTimer } from '@/components/timer/useTimer';
import { TimerDisplay } from '@/components/timer/TimerDisplay';
import { TimerControls } from '@/components/timer/TimerControls';
import { Header } from '@/components/header/Header';
import { SettingsModal } from '@/components/settings/SettingsMordal';
import { useSettings } from '@/contexts/SettingsContext';
import { useTasks } from '@/contexts/TaskContext';

export default function LongBreakPage() {
  const { settings } = useSettings();
  const { tasks, selectedTaskId } = useTasks();
  const INITIAL_TIME = settings.longBreakTime * 60;

  const { timeLeft, isRunning, toggleTimer, resetTimer } = useTimer({
    initialTime: INITIAL_TIME,
    timerType: 'long-break',
  });

  // 設定モーダルの状態
  const [isSettingsOpen, setIsSettingsOpen] = useState(false);

  // 選択された作業項目の名前を取得
  const selectedTask = tasks.find(t => t.id === selectedTaskId);
  const taskName = selectedTask?.name || '項目なし';

  return (
    <div className="flex flex-col min-h-screen bg-black text-white">
      {/* ヘッダー */}
      <Header />

      {/* メインエリア */}
      <div className="flex-1 flex flex-col items-center justify-center gap-8 px-4">
        {/* 作業項目名（円形タイマーの上） */}
        <h2 className="text-xl font-medium text-white tracking-wide">
          {taskName}
        </h2>

        {/* タイマー表示 */}
        <TimerDisplay
          timerType="long-break"
          timeLeft={timeLeft}
          initialTime={INITIAL_TIME}
        />

        {/* ボタンエリア */}
        <TimerControls
          isRunning={isRunning}
          hasStarted={hasStarted}
          onToggle={toggleTimer}
          onReset={resetTimer}
          onOpenSettings={() => setIsSettingsOpen(true)}
        />
      </div>

      {/* 設定モーダル */}
      <SettingsModal
        open={isSettingsOpen}
        onOpenChange={setIsSettingsOpen}
      />
    </div>
  );
}
```

#### 4. 動作確認

```bash
# 開発サーバーで確認
npm run dev
```

- `/timer/focus`、`/timer/break`、`/timer/long-break` の3ページを確認
- ヘッダー、タイマー、ボタンが正しく表示されることを確認
- SETTINGSボタンをクリックしてモーダルが開くことを確認
- START/STOP、RESETボタンが正常に動作することを確認

---

### Step 2.2: 作業記録画面の戻るボタン強化

**所要時間**: 1-2時間

#### 1. records/page.tsx の修正

**ファイルパス**: `app/(public)/records/page.tsx`

**変更箇所1: インポートの追加**

```tsx
// 既存のインポートの後に追加
import { ArrowLeft } from 'lucide-react';
```

**変更箇所2: return文の最初を修正**

```tsx
return (
  <div className="min-h-screen bg-black text-white p-8">
    {/* 戻るボタン（左上）- 新規追加 */}
    <div className="fixed top-6 left-6 z-10">
      <Link
        href="/timer/focus"
        className="flex items-center gap-2 px-4 py-2 bg-gray-800 hover:bg-gray-700 rounded-lg transition-colors text-white shadow-lg"
      >
        <ArrowLeft className="w-5 h-5" />
        <span className="font-medium">タイマーに戻る</span>
      </Link>
    </div>

    {/* メインコンテンツ */}
    <div className="max-w-4xl mx-auto pt-16">
      <h1 className="text-3xl font-bold mb-8">作業記録</h1>

      {/* 本日のサマリー - 既存のコード */}
      <div className="bg-gradient-to-r from-blue-900/50 to-purple-900/50 rounded-xl p-6 mb-8 border border-blue-800/50">
        {/* ... 既存のコード ... */}
      </div>

      {/* 最近の記録 - 既存のコード */}
      <h2 className="text-xl font-semibold mb-4 text-gray-300">最近の記録</h2>
      {/* ... 既存のコード ... */}
    </div>
  </div>
);
```

**変更箇所3: 既存の下部リンクを削除**

以下の部分を**削除**:

```tsx
<div className="mt-8">
  <Link
    href="/timer/focus"
    className="inline-block px-6 py-3 bg-blue-600 hover:bg-blue-700 rounded-lg transition-colors"
  >
    タイマーに戻る
  </Link>
</div>
```

#### 2. 動作確認

- `/records` ページを開き、左上に戻るボタンが表示されることを確認
- 戻るボタンをクリックして `/timer/focus` に遷移することを確認

---

### Step 2 のコミット

```bash
# 変更をコミット
git add app/(public)/timer/focus/page.tsx
git add app/(public)/timer/break/page.tsx
git add app/(public)/timer/long-break/page.tsx
git add app/(public)/records/page.tsx
git commit -m "feat: Phase 2完了 - タイマーページレイアウト変更、作業記録画面の戻るボタン改善"
```

---

## Phase 3: 装飾要素の実装

**期間**: 1月24日-1月26日
**優先度**: 中

---

### Step 3.1: DecorativeElements コンポーネント作成

**所要時間**: 2-3時間

#### 1. ファイル作成

```bash
# components/timer ディレクトリに新しいファイルを作成
touch components/timer/DecorativeElements.tsx
```

#### 2. DecorativeElements.tsx の実装

**ファイルパス**: `components/timer/DecorativeElements.tsx`

```tsx
'use client';

import { Star } from 'lucide-react';

export function DecorativeElements() {
  return (
    <div className="absolute top-20 left-0 right-0 flex justify-center items-center gap-4 opacity-20 pointer-events-none select-none z-0">
      {/* 左の星 */}
      <Star
        className="w-6 h-6 text-blue-300 animate-pulse"
        fill="currentColor"
      />

      {/* Vibe Codingテキスト */}
      <span className="text-blue-200 text-sm tracking-widest font-mono uppercase">
        Vibe Coding
      </span>

      {/* 右の星 */}
      <Star
        className="w-6 h-6 text-purple-300 animate-pulse"
        fill="currentColor"
        style={{ animationDelay: '0.5s' }}
      />
    </div>
  );
}
```

#### 3. タイマーページに追加

**ファイルパス**: `app/(public)/timer/focus/page.tsx`（他のページも同様）

**インポート追加**:

```tsx
import { DecorativeElements } from '@/components/timer/DecorativeElements';
```

**return文の修正**（ヘッダーの直後に追加）:

```tsx
return (
  <div className="flex flex-col min-h-screen bg-black text-white relative">
    {/* ヘッダー */}
    <Header />

    {/* 装飾要素（新規追加） */}
    <DecorativeElements />

    {/* メインエリア */}
    <div className="flex-1 flex flex-col items-center justify-center gap-8 px-4 relative z-10">
      {/* ... 既存のコード ... */}
    </div>

    {/* 設定モーダル */}
    <SettingsModal ... />
  </div>
);
```

**注意**: `break/page.tsx` と `long-break/page.tsx` にも同じ変更を適用。

#### 4. 動作確認

- タイマーページで装飾要素が表示されることを確認
- 装飾が控えめに表示され、タイマーの邪魔にならないことを確認

---

### Step 3.2: 星空エフェクトの追加（オプション）

**所要時間**: 1-2時間

#### 1. globals.css の修正

**ファイルパス**: `app/globals.css`

**ファイル末尾に以下を追加**:

```css
/* 星空エフェクト（オプション） */
.star-field {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  pointer-events: none;
  z-index: 0;
  background-image:
    radial-gradient(2px 2px at 20% 30%, white, transparent),
    radial-gradient(2px 2px at 60% 70%, white, transparent),
    radial-gradient(1px 1px at 50% 50%, white, transparent),
    radial-gradient(1px 1px at 80% 10%, white, transparent),
    radial-gradient(1px 1px at 90% 60%, white, transparent),
    radial-gradient(2px 2px at 33% 85%, white, transparent),
    radial-gradient(1px 1px at 15% 90%, white, transparent);
  background-size: 200px 200px;
  background-repeat: repeat;
  opacity: 0.05;
  animation: twinkle 8s infinite ease-in-out;
}

@keyframes twinkle {
  0%, 100% {
    opacity: 0.03;
  }
  50% {
    opacity: 0.08;
  }
}
```

#### 2. タイマーページに適用（オプション）

**ファイルパス**: `app/(public)/timer/focus/page.tsx`

```tsx
return (
  <div className="flex flex-col min-h-screen bg-black text-white relative">
    {/* 星空エフェクト（オプション） */}
    <div className="star-field" />

    {/* ヘッダー */}
    <Header />

    {/* ... 既存のコード ... */}
  </div>
);
```

**注意**: 星空エフェクトは控えめにし、必要に応じて後で削除・調整可能にする。

---

### Step 3 のコミット

```bash
# 変更をコミット
git add components/timer/DecorativeElements.tsx
git add app/(public)/timer/focus/page.tsx
git add app/(public)/timer/break/page.tsx
git add app/(public)/timer/long-break/page.tsx
git add app/globals.css
git commit -m "feat: Phase 3完了 - 装飾要素（Vibe Coding、星）の実装"
```

---

## Phase 4: レスポンシブ調整

**期間**: 1月26日-1月28日
**優先度**: 中

---

### Step 4.1: TimerDisplay のレスポンシブ調整

**所要時間**: 2-3時間

#### 1. TimerDisplay.tsx の修正

**ファイルパス**: `components/timer/TimerDisplay.tsx`

**変更箇所**: SVG要素のクラス名を修正

**現在**:
```tsx
<svg className="..." viewBox="0 0 360 360">
```

**変更後**:
```tsx
<svg className="w-80 h-80 md:w-96 md:h-96" viewBox="0 0 360 360">
```

**タイマー数字のフォントサイズも調整**:

**現在**:
```tsx
<span className="text-6xl font-bold ...">
```

**変更後**:
```tsx
<span className="text-5xl md:text-6xl font-bold ...">
```

#### 2. 動作確認

- デスクトップ（1920px）で表示を確認
- タブレット（768px）で表示を確認
- モバイル（375px）で表示を確認

---

### Step 4.2: モバイルでのボタン配置調整

**所要時間**: 1-2時間

#### 1. 確認事項

`TimerControls.tsx` で既に以下のレスポンシブクラスを使用しているか確認:

```tsx
<div className="grid grid-cols-1 md:grid-cols-2 gap-4 w-full max-w-md px-4">
```

- `grid-cols-1`: モバイル時に縦並び
- `md:grid-cols-2`: タブレット以上で2列

#### 2. order クラスの確認

モバイル時に START/STOP ボタンを上に表示するため:

```tsx
<div className="flex flex-col gap-3 order-2 md:order-1">
  {/* SETTINGS / RESET */}
</div>

<div className="flex items-stretch order-1 md:order-2">
  {/* START/STOP */}
</div>
```

#### 3. 動作確認

- モバイルで START/STOP が上、SETTINGS/RESET が下に表示されることを確認
- タブレット・デスクトップで2列表示になることを確認

---

### Step 4.3: タッチデバイス最適化

**所要時間**: 1時間

#### 1. 確認事項

ボタンの最小タップエリアが44px以上であることを確認:

- SETTINGS/RESET: `h-14`（56px）→ OK
- START/STOP: `min-h-[132px]`（132px）→ OK

#### 2. ホバー・アクティブ状態の確認

`active:scale-95` クラスが追加されていることを確認（タッチフィードバック）。

---

### Step 4 のコミット

```bash
# 変更をコミット
git add components/timer/TimerDisplay.tsx
git add components/timer/TimerControls.tsx
git commit -m "feat: Phase 4完了 - レスポンシブ対応、モバイル最適化"
```

---

## 6. テスト手順

**期間**: 1月28日-1月30日

### 6.1 機能テスト

#### チェックリスト

```markdown
- [ ] タイマー画面でヘッダーが正しく表示される
- [ ] ユーザーアイコンクリックで `/records` へ遷移
- [ ] 作業項目名が動的に表示される（選択時/未選択時）
- [ ] SETTINGSボタンクリックでモーダルが開く
- [ ] モーダル内で作業項目選択が可能
- [ ] START/STOPボタンが正しく動作
- [ ] RESETボタンが正しく動作
- [ ] セッションカウントがfocus時のみ表示される
- [ ] 装飾要素（Vibe Coding、星）が表示される
- [ ] 作業記録画面の戻るボタンが機能する
- [ ] タイマー完了時の自動遷移が正常
- [ ] 作業記録が正しく保存される
```

### 6.2 レスポンシブテスト

#### デバイスサイズ別チェック

**デスクトップ（1920px以上）**:
```markdown
- [ ] 2列ボタンレイアウト（左: SETTINGS/RESET、右: START/STOP）
- [ ] タイマーが大きく表示される（384px）
- [ ] ヘッダー要素が適切に配置される
```

**タブレット（768px-1024px）**:
```markdown
- [ ] 2列ボタンレイアウトが維持される
- [ ] タイマーサイズが調整される
- [ ] テキストが読みやすい
```

**モバイル（375px-768px）**:
```markdown
- [ ] ボタンが縦並びになる（START/STOP → SETTINGS/RESET）
- [ ] タイマーが小さく表示される（320px）
- [ ] タッチターゲットが十分な大きさ（44px以上）
- [ ] スクロールなしで全要素が見える
```

### 6.3 ブラウザ互換性テスト

```markdown
- [ ] Chrome（最新版）
- [ ] Firefox（最新版）
- [ ] Safari（最新版）
- [ ] Edge（最新版）
```

### 6.4 統合テスト

```markdown
- [ ] ログイン → タイマー実行 → 作業記録保存 → 記録画面表示
- [ ] タイマー実行中に設定変更不可
- [ ] セッションカウントが正確（1→2→3→4→long-break→リセット）
- [ ] 通知音が正常に再生される
- [ ] ログアウト → ログインで設定が保持される
```

### 6.5 バグ修正

テストで見つかったバグは以下の手順で修正:

1. 問題を特定
2. 該当ファイルを修正
3. 再テスト
4. コミット

```bash
git add [修正したファイル]
git commit -m "fix: [バグの説明]"
```

---

## 7. デプロイ手順

**期限**: 1月31日

### 7.1 最終確認

```bash
# ビルドエラーがないか確認
npm run build

# ESLintエラーがないか確認
npm run lint
```

エラーがある場合は修正。

### 7.2 プルリクエストの作成

```bash
# すべての変更をプッシュ
git push origin feature/layout-redesign
```

GitHubでプルリクエストを作成:

**タイトル**: `feat: ポモドーロタイマーのレイアウト変更`

**説明**:
```markdown
## 概要
画像デザインに基づき、ポモドーロタイマーのレイアウトを全面的に変更しました。

## 主な変更点
- タイマー画面にヘッダー追加（ユーザーアイコン、作業項目名表示）
- ボタンレイアウトを2列に変更（SETTINGS/RESET、START/STOP）
- 設定モーダルに作業項目選択を統合
- 装飾要素追加（Vibe Coding、星）
- レスポンシブ対応（デスクトップ・モバイル）

## テスト結果
- 機能テスト: ✅
- レスポンシブテスト: ✅
- ブラウザ互換性: ✅
- 統合テスト: ✅

## スクリーンショット
（デスクトップ・モバイルのスクリーンショットを添付）

## 関連Issue
#[Issue番号]
```

### 7.3 レビュー対応

レビューコメントがあれば対応:

```bash
# 修正
git add [修正したファイル]
git commit -m "fix: レビュー指摘対応 - [内容]"
git push origin feature/layout-redesign
```

### 7.4 マージとデプロイ

1. プルリクエストを `main` ブランチにマージ
2. Vercelが自動的に本番環境にデプロイ
3. 本番環境で動作確認

```bash
# 本番環境URL（Vercel）で確認
# https://[your-app].vercel.app/timer/focus
```

### 7.5 完了報告

プロジェクト管理ツールまたはSlackで完了を報告:

```markdown
ポモドーロタイマーのレイアウト変更が完了しました。

✅ Phase 1: コンポーネント構造変更
✅ Phase 2: レイアウト実装
✅ Phase 3: 装飾要素の実装
✅ Phase 4: レスポンシブ調整
✅ テスト・検証
✅ デプロイ

本番環境URL: https://[your-app].vercel.app
```

---

## 8. トラブルシューティング

### 問題1: TypeScriptエラー

**症状**: Props の型エラー

**解決策**:
```tsx
// TimerControls の Props を確認
interface TimerControlsProps {
  isRunning: boolean;
  onToggle: () => void;
  onReset: () => void;
  onOpenSettings: () => void; // 追加されているか確認
}
```

### 問題2: モーダルが開かない

**症状**: SETTINGSボタンをクリックしてもモーダルが開かない

**解決策**:
```tsx
// State が定義されているか確認
const [isSettingsOpen, setIsSettingsOpen] = useState(false);

// onOpenSettings が正しく渡されているか確認
<TimerControls
  onOpenSettings={() => setIsSettingsOpen(true)}
/>
```

### 問題3: レスポンシブが機能しない

**症状**: モバイルで2列のまま表示される

**解決策**:
```bash
# Tailwind設定を確認
npm run dev

# ブラウザのキャッシュをクリア
Ctrl+Shift+R（Windows）/ Cmd+Shift+R（Mac）
```

---

## 9. まとめ

この作業手順書に従って、4つのフェーズで段階的に実装を進めてください。

- **Phase 1 & 2**: 基本的な機能とレイアウト（必須）
- **Phase 3**: 装飾要素（推奨）
- **Phase 4**: レスポンシブ対応（必須）

期限は2026年1月31日です。計画的に進めましょう。

---

**文書バージョン**: 1.0
**作成日**: 2026年1月17日
**最終更新日**: 2026年1月17日
